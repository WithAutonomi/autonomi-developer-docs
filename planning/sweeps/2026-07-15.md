# Upstream sweep — 2026-07-15

Daily upstream-drift sweep. Metadata-only stamp refreshes for pages whose audited upstream changes carry no developer-facing impact on the documented surface. Only `ant-sdk` moved since the previous sweep (2026-07-13); every other in-scope repo is unchanged.

## Repos checked

| Repo | Range | Classification |
|------|-------|----------------|
| ant-sdk | 9ff4ca8 → cea1b7a | metadata-only |
| ant-client | bcab72a → bcab72a | no drift |
| ant-node | bc4c448 → bc4c448 | no drift |
| ant-protocol | 83d588e → 83d588e | no drift |
| self_encryption | 0deb040 → 0deb040 | no drift |
| evmlib | 28fc354 → 28fc354 | no drift |
| ant-keygen | — | no drift |
| ant-merkle | — | no drift |
| saorsa-core | — | no drift |
| saorsa-pqc | — | no drift |
| saorsa-transport | — | no drift |

## Developer-facing change

- **ant-sdk**: the range touches only two files in the upstream repo's own `docs/` tree. `docs/external-signer-flow.md` gains a clarifying title suffix and a callout distinguishing the antd daemon REST flow from the mobile flow; its documented REST endpoints and two-phase upload steps are unchanged. `docs/mobile-external-signer.md` is a new document covering the in-process mobile FFI SDK (`ant-swift` / `ant-android`, `ant-mobile-ios` / `ant-mobile-android`), which is not an in-scope repo in `repo-registry.yml`. The REST/gRPC daemon surface (`antd/openapi.yaml`, `antd/proto/`), the thin-client binding packages, and the CLI are byte-identical across the range, so no stamped SDK, MCP, guide, or language-binding page changed.

## Target-manifest entries

None. No `target-manifest` blocks were skipped or bumped.

## Pages stamped

Verification blocks refreshed to the audited head SHA (`cea1b7a`) across the 48 docs pages that pin `ant-sdk`, plus the `verified_commits.ant-sdk` entries in `skills/start/version.json` and `skills/start/SKILL.md` frontmatter. Only `source_commit:` and `verified_date:` lines inside `ant-sdk` verification blocks moved; no rendered prose, no skill body, and no release metadata changed.
