# Sweep — 2026-08-04

## Branch

`claude/sweep-2026-08-04`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-node | c8f0ce1 | 0c6977a |
| saorsa-pqc | bb2aa19 | 4fbb31d |

## Classification

All records on the 11 pages updated here are **metadata-only**. Neither diff touches a developer-facing surface — no documented command, endpoint, type, field, code sample, CLI flag, public Rust module, or README changed on any recorded page.

- **ant-node** (`c8f0ce1 → 0c6977a`, PR #181): a change confined to the internal node-to-node storage audit and replication subsystem (`src/replication/` — audit, storage commitment audit, slice verification, protocol families), plus the `docs/REPLICATION_DESIGN.md` design note and a new internal ADR. The audit digest construction moved to a BLAKE3 keyed-hash derived-key form and audit message families were split off the core replication protocol id. It adds a `bao` dependency for verified-slice streaming. No CLI, public library, binary entry point, or README surface changed. No recorded page documents the storage audit protocol, possession probes, digest construction, or replication internals; the one high-level mention (`system-overview.md` lists replication among the node's responsibilities) remains accurate.
- **saorsa-pqc** (`bb2aa19 → 4fbb31d`, PR #5): dependency-advisory cleanup (removing unused `honggfuzz` and `pprof` dev-dependencies to clear RustSec advisories) plus added ADR governance tooling and docs. No `src/` change. The ML-DSA-65 signature, ML-KEM-768 key-encapsulation, and key-derivation surfaces documented on the recorded pages are unchanged.

No prose PR and no manual-review issues were opened.

## Pages updated (11)

Every drifted `<!-- verification: ... -->` block on these pages had its `source_commit` and `verified_date` refreshed. Non-drifted blocks (for example the `ant-sdk`, `ant-client`, `ant-protocol`, `saorsa-core`, `saorsa-transport`, `evmlib`, `self_encryption`, and `ant-keygen` blocks on the multi-repo pages) were left byte-identical. No prose changed.

- `docs/architecture/system-overview.md` (ant-node, saorsa-pqc)
- `docs/core-concepts/overview.md` (saorsa-pqc)
- `docs/core-concepts/payment-model.md` (ant-node)
- `docs/core-concepts/post-quantum-cryptography.md` (saorsa-pqc)
- `docs/guides/overview.md` (ant-node)
- `docs/guides/set-up-a-local-network.md` (ant-node)
- `docs/guides/test-your-application.md` (ant-node)
- `docs/reference/glossary.md` (ant-node, saorsa-pqc)
- `docs/reference/source-repositories.md` (ant-node, saorsa-pqc)
- `docs/rust/embed-a-node-in-your-application.md` (ant-node)
- `docs/sdk/start-the-local-daemon.md` (ant-node)

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-node` `verified_commits` value refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep). `saorsa-pqc` is not tracked in the skill's `verified_commits`, so no skill change was needed for that bump.
