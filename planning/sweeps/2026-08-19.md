# Sweep — 2026-08-19

## Branch

`claude/sweep-2026-08-19`

This run opened both a sweep PR (this summary) and a prose PR
(`claude/prose-2026-08-19-merkle-multibatch-and-cli`). The sweep PR carries the
metadata-only stamps; the prose PR carries the pages whose documented surface
changed. The two PRs never touch the same page.

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-sdk | 3264b51 | 8378338 |
| ant-client | 81a0a24 | 50b4370 |
| ant-node | fe2d43d | 661e68a |
| ant-protocol | 54c93a8 | 2bd604a |
| evmlib | 88e20df | fbf879b |
| saorsa-core | 043391c | a08750c |
| saorsa-transport | 24f18c3 | ace6d07 |

## Classification

Two developer-facing surfaces changed and are handled entirely in the prose PR:
multi-batch external-signer merkle payment plus the new `PARTIAL_UPLOAD` error
(ant-sdk and ant-client), and the `ant update --channel` self-update flag
(ant-client). Every other drifted record across the seven repos is
**metadata-only** and is stamped here. No manual-review issues were opened — no
record was ambiguous.

- **ant-sdk** (`3264b51 → 8378338`): the `chore/bump-0.12.0` merge adds the
  multi-batch external-signer merkle surface (`merkle_batches` on the prepare
  response, `winner_pool_hashes` on finalize) and the `PARTIAL_UPLOAD` error
  across the REST, gRPC, and FFI layers. Those surfaces are documented on
  `use-external-signers-for-upload-payments.md`, `rest-api.md`, and
  `grpc-services.md`, which the prose PR updates. The MCP crate (`antd-mcp/`)
  is byte-identical in this window, so `mcp-server-reference.md` is
  metadata-only. Every other ant-sdk-citing page (language bindings, install,
  daemon command reference, store/retrieve pages) documents surfaces the diff
  does not touch, so those are metadata-only.
- **ant-client** (`81a0a24 → 50b4370`): the `v2-1013` merge adds the
  `ant update --channel <stable|beta>` self-update flag and the ant-core
  multi-batch external merkle helpers (`prepare_merkle_batches_external`,
  `finalize_upload_merkle_multi`, and the `ExternalPaymentInfo::Merkle` field
  reshape). `command-reference.md`, `library-reference.md`, and
  `use-external-signers-for-upload-payments.md` document those and are in the
  prose PR. The node upgrade-channel flags (`ant node add --upgrade-channel`,
  `--version`, `--url`) were already documented at the recorded SHA, so no
  drift there. `use-the-cli.md` documents neither the update flag nor the
  external-signer helpers, so it is metadata-only.
- **ant-node** (`fe2d43d → 661e68a`): the window adds beta upgrade-channel
  semantics, a LAN Anvil bind for `ant-devnet --host --enable-evm`, replication
  and storage-handler internals, and an h2 RustSec dependency bump. None of
  these are documented surfaces: the local-devnet pages drive the devnet through
  `ant dev start` and never document the `ant-devnet --host`/`ANVIL_IP_ADDR`
  binding, and the embed-a-node guide pins the published crate version, which is
  unaffected. Metadata-only.
- **ant-protocol** (`54c93a8 → 2bd604a`): a version bump to `2.3.2` with
  `saorsa-core` and `evmlib` dependency-pin refreshes in `Cargo.toml`/`Cargo.lock`.
  No wire-format, payment-proof, or devnet-manifest type changed. Metadata-only.
- **evmlib** (`88e20df → fbf879b`): a release version bump to `0.9.1` in
  `Cargo.toml`. No payment, wallet, or external-signer API changed. Metadata-only.
- **saorsa-core** (`043391c → a08750c`): internal DHT, reachability, and
  routing-table-snapshot work (canary-gated proactive relays, close-group cache
  validity, NAT-traversal driver changes) plus a `saorsa-transport` dependency
  bump. No routing, trust, or identity claim documented on the architecture,
  glossary, or source-repository pages changed. Metadata-only.
- **saorsa-transport** (`24f18c3 → ace6d07`): internal NAT-traversal, MASQUE
  relay, and keep-alive dial/accept-split work. No transport-layer claim
  documented on the post-quantum-cryptography, system-overview, index, or
  glossary pages changed. Metadata-only.

## Pages updated (49 pages, 85 blocks)

For each drifted page, only the `<!-- verification: ... -->` block(s) whose
`source_commit` matched one of the seven recorded SHAs had their `source_commit`
and `verified_date` refreshed. Blocks for non-drifted repos (for example the
`self_encryption` blocks and any `saorsa-pqc` blocks on the multi-repo pages)
were left byte-identical. The five prose-PR pages
(`use-external-signers-for-upload-payments.md`, `rest-api.md`,
`grpc-services.md`, `library-reference.md`, `command-reference.md`) are not
touched by this PR. No prose changed here.

## Skill

`skills/start/version.json` and the `skills/start/SKILL.md` frontmatter had the
`ant-sdk`, `ant-client`, `ant-node`, `ant-protocol`, and `evmlib` values in
their `verified_commits` maps refreshed (key set unchanged; `self_encryption`
did not drift). The `SKILL.md` body treats prepare/finalize payloads, merkle
response shapes, and CLI flags as volatile ("fetch the live reference"), and
names no surface that changed in these windows, so the body is unchanged. The
`version`, `published_date`, and `CHANGELOG.md` are unchanged — this is a
metadata-only skill stamp, so the linked skill release is not triggered.

## Target-manifest

No `target-manifest` blocks are active; nothing was skipped on that basis.
