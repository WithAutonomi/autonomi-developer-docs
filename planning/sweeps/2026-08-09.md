# Sweep — 2026-08-09

## Branch

`claude/sweep-2026-08-09`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-sdk | 26ae484 | 3264b51 |
| ant-node | 20db1f9 | fe2d43d |
| self_encryption | 0deb040 | 4021f66 |
| evmlib | 9331387 | 88e20df |
| ant-keygen | 3a2953f | 902442f |
| saorsa-core | ea8124b | 043391c |
| ant-merkle | 80af80a | 176ab0a |

## Classification

All 84 drifted records across the seven repos above are **metadata-only**. Every diff in each recorded-to-head window is confined to relicensing, README/badge metadata, CI-workflow text, and dependency/version plumbing. No documented command, endpoint, type, field, code sample, CLI flag, public Rust module, REST payload, gRPC service, or live-reference URL changed on any recorded page, so no prose PR and no manual-review issues were opened.

- **ant-sdk** (`26ae484 → 3264b51`): a release-candidate dependency bump only — `antd/Cargo.toml`, `ffi/rust/ant-ffi/Cargo.toml`, and their `Cargo.lock` files move the `ant-core` pin from tag `ant-cli-v0.3.1` (core 0.5.0) to `ant-cli-v0.3.2` (core 0.5.1) and the transitive `ant-protocol` pin from `2.3.0` to `2.3.1`. No OpenAPI spec, `.proto` file, CLI surface, or binding API changed. The docs do not cite the antd release-candidate version, `ant-core`, or `ant-protocol` version numbers in rendered prose, so the bump is invisible to the pages.
- **ant-node** (`20db1f9 → fe2d43d`): the window touches only `.github/workflows/release.yml` and a comment in the `[features]` section of `Cargo.toml`. The `ant-node` crate version line did not change in this window (it is `0.17.0` on both sides). The `docs/rust/embed-a-node-in-your-application.md` guidance pins the crates.io published `ant-node = "0.11.0"`, which is unaffected.
- **self_encryption** (`0deb040 → 4021f66`): relicense from GPL-3.0 to dual MIT OR Apache-2.0 — license files, per-file license headers, `Cargo.toml`/`pyproject.toml`/`package.json` license metadata, and README badge/link refresh. The crate version is unchanged (`0.36.0`). No public encryption API changed.
- **evmlib** (`9331387 → 88e20df`): relicense to dual MIT OR Apache-2.0 — added license files, per-file header swaps, and `Cargo.toml` license metadata. No payment, wallet, or external-signer API changed.
- **ant-keygen** (`3a2953f → 902442f`): added `LICENSE-APACHE` and `LICENSE-MIT` only.
- **saorsa-core** (`ea8124b → 043391c`): relicense from AGPL-3.0 to dual MIT OR Apache-2.0 — license files, per-file header swaps, README license section, and a `SECURITY_MODEL.md` SPDX line. No routing, DHT, trust, or identity type or behaviour changed.
- **ant-merkle** (`80af80a → 176ab0a`): relicense to dual MIT OR Apache-2.0 — added `LICENSE-APACHE`, renamed `LICENSE.md` to `LICENSE-MIT`, README license section, and a `Cargo.toml` license field.

## Pages updated (51 pages, 76 blocks)

For each drifted page, only the `<!-- verification: ... -->` block(s) whose `source_commit` matched one of the seven recorded SHAs had their `source_commit` and `verified_date` refreshed. Non-drifted blocks (for example the `ant-client`, `ant-protocol`, `saorsa-transport`, and `saorsa-pqc` blocks on the multi-repo pages) were left byte-identical. No prose changed.

## Skill

`skills/start/version.json` and the `skills/start/SKILL.md` frontmatter had the `ant-sdk`, `ant-node`, `self_encryption`, and `evmlib` values in their `verified_commits` maps refreshed (key set unchanged; the `ant-client` and `ant-protocol` values did not drift). The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged — this is a metadata-only sweep, so the linked skill release is not triggered.

## Note on the date suffix

The real audit date is 2026-08-08. The `planning/sweeps/2026-08-08.md` slot was already consumed by an earlier merged sweep (the immediately preceding `ant-node` bump `9a50c6b → 20db1f9`), so this run takes the next free date suffix, `2026-08-09`, to satisfy `sweep-guard`'s requirement that the summary file be added at `planning/sweeps/<branch-date>.md`. The `verified_date:` stamps on the bumped blocks carry the real audit date, `2026-08-08`.
