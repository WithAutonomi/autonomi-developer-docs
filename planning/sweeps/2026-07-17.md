# Sweep — 2026-07-17

## Branch

`claude/sweep-2026-07-17`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-sdk | cea1b7ad | d1f3cb03 |
| ant-node | bc4c4481 | 571868a1 |

## Classification

All records on the 49 pages updated here are **metadata-only**. The upstream changes carry no developer-facing prose impact on any recorded page.

- **ant-sdk** (`cea1b7ad → d1f3cb03`): dual-licensing (`MIT OR Apache-2.0`) with added `LICENSE-APACHE`/`LICENSE-MIT`, an `antd` version bump `0.11.0 → 0.11.1`, and a new `installers/` tree of desktop-installer build scripts (Linux deb/rpm + `install.sh`, macOS `.pkg`, Windows MSI). None of these contradict a recorded page: the install and daemon pages document the build-from-source flow, which is unchanged, and no recorded page pins a version number or references a published installer artifact.
- **ant-node** (`bc4c4481 → 571868a1`): a large replication/pruning audit refactor (internal node behaviour), a version bump to `0.14.4`, and an additive opt-in `ant-devnet` LAN / external-devnet mode (`--host`, `--evm-network`, `--serve-port`). The LAN flags default off with an explicit backward-compatibility contract: a devnet started without them binds loopback and stamps `127.0.0.1` into the manifest exactly as before. No recorded page references those flags — the local-network pages drive the devnet through the `ant dev` wrapper (part of ant-sdk, unchanged in this range).

No prose PR and no manual-review issues were opened.

## Pages updated (49)

Every drifted `<!-- verification: ... -->` block on these pages had its `source_commit` and `verified_date` refreshed. Non-drifted blocks (for example the `ant-client` block on `test-your-application.md`) were left byte-identical. No prose changed.

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-sdk` and `ant-node` `verified_commits` values and `verified_date` refreshed. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep).

## Follow-up (not part of this sweep)

The opt-in `ant-devnet` LAN / external-devnet mode is a genuinely new testing capability that no page documents yet. A future prose change could add a how-to for LAN devnet testing (reaching a devnet from a phone, simulator, or second machine, backed by Arbitrum Sepolia, with the manifest served over HTTP). Out of scope for a metadata-only sweep.
