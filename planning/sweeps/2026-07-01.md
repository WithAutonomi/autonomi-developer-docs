# Upstream sweep — 2026-07-01

Daily upstream-drift sweep. Metadata-only stamp refreshes for pages whose audited upstream changes carry no developer-facing impact. The `ant node add` CLI flag change on `ant-client` is handled separately in the prose PR for `docs/cli/command-reference.md`.

## Repos checked

| Repo | Range | Classification |
|------|-------|----------------|
| ant-client | 3ab50c4 → 95a23be | prose for `docs/cli/command-reference.md` (separate PR); metadata-only for all other pages |
| ant-node | e53928a → 3df630c | metadata-only |
| ant-protocol | 7caae77 → 82cab84 | metadata-only |
| saorsa-core | ead6718 → ef00920 | metadata-only |

## Developer-facing change

- **ant-client**: internal chunk PUT fallback and quote handling refinements (ADR-0002) with no documented-surface change on the stamped pages. The `ant node add` flag surface change is documented in the prose PR.
- **ant-node**: replication and storage internals (full-node detection, delayed possession checks, self-closeness gate on client PUTs, fresh-replication delivery retries — ADR-0003). No developer-facing documented surface changed.
- **ant-protocol**: release version bump only (2.2.2); no source changes.
- **saorsa-core**: reconnect-on-demand for `P2PNode::send_request`; internal networking behaviour with no documented-surface change.

## Target-manifest entries

None. No `target-manifest` blocks were skipped or bumped.

## Pages stamped

Verification blocks refreshed to the audited head SHAs across architecture, CLI (`use-the-cli`), core-concepts, guides, reference, rust, and sdk pages, plus the `verified_commits` maps in `skills/start/version.json` and `skills/start/SKILL.md`.
