# Sweep — 2026-07-22

## Branch

`claude/sweep-2026-07-22`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-sdk | d1f3cb03 | 71c84b90 |
| ant-node | 1b04edd1 | eb74b564 |

## Classification

All records on the 49 pages updated here are **metadata-only**. The upstream changes carry no developer-facing prose impact on any recorded page.

- **ant-sdk** (`d1f3cb03 → 71c84b90`, PR #224): the diff is confined to `ffi/` and `.github/workflows/ffi-ci.yml` — the mobile C-ABI FFI layer and its packaging CI. It renames and extends FFI methods (`connect_default`/`connect_default_with_wallet`/`connect_default_for_external_signer`, an optional `data_dir` on the connect calls, `_with_progress` file variants, an `ant_ffi_version` free function, and a `TxKind` enum replacing the string `kind` on external-signer transactions). No recorded page documents the FFI surface: the language-binding pages describe the daemon REST/gRPC clients (`AntdClient`), not the `ant-ffi` C ABI, and the `antd` daemon, OpenAPI spec, gRPC proto, and CLI surfaces are untouched in this range. The `data_put_public`-style names that appear in the docs are daemon-client and `ant-core` methods, not FFI exports.
- **ant-node** (`1b04edd1 → eb74b564`, PR #176): the receiver-side revenue floor from ADR-0006 (Proposed). It adds a shadow-mode-by-default price-floor check on single-node store admissions, enabled per node through the `ANT_PRICE_FLOOR_ENFORCE` and `ANT_PRICE_FLOOR_TOLERANCE_PERCENT` environment variables (`PriceFloorConfig::from_env`), plus supporting verifier, quote, replication, and storage wiring. It is node-side economic policy that is off by default and read from the environment rather than a `NodeConfig` field. No recorded page documents node payment-verifier configuration or these environment variables; the payment-model claim that nodes verify the payment proof on receipt remains accurate, and the embed-a-node config list explicitly disclaims completeness.

No prose PR and no manual-review issues were opened.

## Pages updated (49)

Every drifted `<!-- verification: ... -->` block on these pages had its `source_commit` and `verified_date` refreshed. Non-drifted blocks (for example the `ant-client`, `evmlib`, `self_encryption`, and `ant-protocol` blocks on the multi-repo reference and payment pages) were left byte-identical. No prose changed.

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-sdk` and `ant-node` `verified_commits` values refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep).
