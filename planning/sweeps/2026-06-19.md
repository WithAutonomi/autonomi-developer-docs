# Upstream sweep — 2026-06-19

## Drifted repos

| Repo | Recorded SHA | Head SHA | Classification |
|------|-------------|---------|----------------|
| ant-sdk | `e102df9b3ea1a17fba7cf731081f515d89552b82` | `e56292325d04f1cf398c7e6bc77619ff2ab44447` | prose + metadata-only |
| ant-client | `84332e2d752499df5402da797e5d8bba547bc58b` | `4d0448458ec302af68a5504c533d105b0991c93c` | prose + metadata-only |
| ant-node | `fb7494e84dd02f664fce83cebd210a4c73ddcfe2` | `c53665bf4dda87cdbcf848606fa0182e822297d7` | prose + metadata-only |

Not drifted: `ant-protocol`, `saorsa-core`, `saorsa-transport`, `saorsa-pqc`, `self_encryption`, `evmlib`, `ant-merkle`, `ant-keygen`.

Pinned by target-manifest, not bumped: none (no active target-manifest).

## Developer-facing impact

- **ant-sdk** (antd 0.9.2 → 0.10.0): adds streaming downloads (gRPC `Stream` RPC and an implemented `StreamPublic`, REST `POST /v1/data/stream` and an implemented `GET /v1/data/public/{addr}/stream`, both with optional NDJSON progress framing), adds `chunks_stored` and `payment_mode_used` to the data `Put`/`PutPublic` responses, adds `total_chunks` and `already_stored_count` to the external-signer prepare response, and adds a `stream_download_file` MCP tool (now 19 tools). The bindings gain matching streaming methods and model fields. Prose-impacting for the REST, gRPC, MCP, and external-signer pages; the remaining ant-sdk pages document surfaces that did not change.
- **ant-client** (release rc-2026.6.2): adds an optional `--peers` flag (alias `peer-count`) to `ant file download` for selecting how many closest peers to try per chunk fetch, and changes `ant file cost` so an estimate where every sampled chunk is already stored reports "free" instead of failing. Other changes (persistent client peer cache, public-upload result plumbing) are internal. Prose-impacting for the CLI command reference; the remaining ant-client pages document surfaces that did not change.
- **ant-node** (release rc-2026.6.2): removes the node bootstrap-cache configuration (`--disable-bootstrap-cache`, `--bootstrap-cache-dir`, `--bootstrap-cache-capacity`, and the `bootstrap_cache` `NodeConfig` field). Remaining changes (replication, payment verifier, bootstrap-peers discovery) are internal. Prose-impacting for the embedded-node page, which listed the removed config field.

## Outcome

This run opened a metadata-only sweep PR and a prose PR. The two PRs touch disjoint page sets.

Stamped in this sweep PR (metadata-only — 47 docs pages plus the developer skill):

- the 47 docs pages whose drifted source repos changed only internally relative to their documented surface
- `skills/start/version.json` (`verified_commits` for ant-sdk, ant-client, ant-node)
- `skills/start/SKILL.md` frontmatter (`verified_commits` for ant-sdk, ant-client, ant-node, and `verified_date`)

Handled in the prose PR (rendered-text edits plus their own stamps):

- `docs/sdk/reference/rest-api.md`
- `docs/sdk/reference/grpc-services.md`
- `docs/mcp/mcp-server-reference.md`
- `docs/sdk/how-to-guides/use-external-signers-for-upload-payments.md`
- `docs/cli/command-reference.md`
- `docs/rust/embed-a-node-in-your-application.md`

The developer skill body was re-audited and left unchanged: it lists only stable surfaces and defers exact schemas, CLI flags, and tool names to the live references, so no body claim went stale. No skill patch release accompanies this run.

Manual-review issues: none (no ambiguous records).
