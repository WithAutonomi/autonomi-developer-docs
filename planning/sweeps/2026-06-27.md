# Upstream sweep — 2026-06-27

## Drifted repos

| Repo | Recorded SHA | Head SHA | Classification |
|------|-------------|---------|----------------|
| ant-node | `0be9579c679e07fa266207bb9af5a4dde1aaee5a` | `e53928ac8eeb33cb07576dae06dbac5f1cf94f18` | metadata-only |
| ant-protocol | `ca95e6e1951ed622bd7d594e3211cee59e2eb3d8` | `7caae7701a52f7ec557dea394abf60c33670521e` | metadata-only |
| saorsa-core | `fe74f93ab9e947ea08c1c0a168205b7011a4383a` | `ead67187b912118fc7d43ca1e8d01938761d5682` | metadata-only |
| saorsa-transport | `e24ecaced902f668d64cda2269deef0babd5d9ab` | `a24065b83c188a2b74cd8a0ad2c518f076016756` | metadata-only |

Not drifted: `ant-sdk`, `ant-client`, `self_encryption`, `evmlib`, `ant-keygen`, `ant-merkle`, `saorsa-pqc`.

Pinned by target-manifest, not bumped: none (no active target-manifest).

## Developer-facing impact

- **ant-node** (5 commits): release cut `0.14.1` plus a `quinn-proto` bump to `0.11.15` for RUSTSEC-2026-0185. Only `Cargo.toml` and `Cargo.lock` changed. No documented surface affected.
- **ant-protocol** (4 commits): release cut `2.2.1`. Only `Cargo.toml` and `Cargo.lock` changed. No documented surface affected.
- **saorsa-core** (12 commits): release cut `0.26.1` plus internal DHT and transport fixes — clearing dial-failure suppression on a newer published address set, peer-scoped publish self-heal, seeding transport TLS identity from the persistent `NodeIdentity` (ADR-011), and the `quinn-proto` RUSTSEC bump. The documented high-level claims (P2P node, DHT, bootstrap handling, trust system, routing-table logic, publishing dialable self-addresses) still hold. No documented surface affected.
- **saorsa-transport** (9 commits): release cut `0.35.1` plus stable relay-port reservations (ADR-011, Proposed) and a fix to tolerate ICMP-driven UDP receive errors in the endpoint driver. These are internal relay and NAT-traversal robustness changes; the documented claims (QUIC transport, NAT traversal, relay-assisted fallback, post-quantum transport security) still hold. No documented surface affected.

## Outcome

11 docs pages stamped (metadata-only): `docs/architecture/system-overview.md`, `docs/core-concepts/payment-model.md`, `docs/core-concepts/post-quantum-cryptography.md`, `docs/guides/overview.md`, `docs/guides/set-up-a-local-network.md`, `docs/guides/test-your-application.md`, `docs/index.md`, `docs/reference/glossary.md`, `docs/reference/source-repositories.md`, `docs/rust/embed-a-node-in-your-application.md`, `docs/sdk/start-the-local-daemon.md`. The skill `verified_commits` entries for `ant-node` and `ant-protocol` and the `verified_date` were refreshed in `skills/start/version.json` and `skills/start/SKILL.md`. No prose changed; no manual-review issues opened.

## Suggested follow-up (outside the sweep envelope)

During the audit, `saorsa-core` and `saorsa-transport` were found to have been transferred from the `saorsa-labs` GitHub organization to `WithAutonomi` (their canonical `full_name` is now `WithAutonomi/saorsa-core` and `WithAutonomi/saorsa-transport`; old URLs still redirect). `saorsa-pqc` remains at `saorsa-labs`. This affects:

- `repo-registry.yml` `url:` fields for `saorsa-core` and `saorsa-transport` (forbidden in the sweep envelope),
- live-reference links in `docs/reference/source-repositories.md` (including the `## saorsa-labs` section heading, which is now mixed), `docs/architecture/system-overview.md`, and `docs/core-concepts/post-quantum-cryptography.md`.

This transfer is orthogonal to the SHA drift detected here and a coherent fix spans `repo-registry.yml` and the docs, so it is left for a human to address in a separate PR.
