# Sweep — 2026-07-27

## Branch

`claude/sweep-2026-07-27`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-client | 2498915b | 5cec8579 |

## Classification

All records on the 19 pages updated here are **metadata-only**. The upstream change carries no developer-facing prose impact on any recorded page.

- **ant-client** (`2498915b → 5cec8579`, PR #159 `rc-2026.7.3`): the diff is confined to `Cargo.lock`, `ant-cli/Cargo.toml`, `ant-core/Cargo.toml`, and `ant-core/tests/support/mod.rs`. It is a release-candidate version bump — `ant-cli` `0.3.0 → 0.3.1`, `ant-core` `0.4.0 → 0.5.0`, and the `ant-node` dependency `0.14.3 → 0.15.0` — plus a test-only harness change. The harness change adds `ant_node::payment::PriceFloorConfig` to the `MiniTestnet` payment config and pins it to `PriceFloorConfig::default()` in shadow mode (`enforce: false`), keeping node admissions behaving as they did before the upstream price-floor addition. `PriceFloorConfig` is a test-support symbol only; it does not appear in any recorded docs page or in `SKILL.md`, and there is no price-floor prose anywhere in the docs set. No public CLI command, flag, `--help` surface, or Rust library type changed in this range. The `version` field shown in the `docs/cli/command-reference.md` node-status example is an illustrative payload value, not a pinned release-version claim.

No prose PR and no manual-review issues were opened.

## Pages updated (19)

Every drifted `<!-- verification: ... -->` block on these pages had its `source_commit` and `verified_date` refreshed. The non-`ant-client` blocks (for example the `ant-sdk`, `ant-node`, `ant-protocol`, `evmlib`, `self_encryption`, `saorsa-core`, and `saorsa-transport` blocks on the multi-repo reference, architecture, index, and payment pages) were left byte-identical. No prose changed.

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-client` `verified_commits` value refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep).
