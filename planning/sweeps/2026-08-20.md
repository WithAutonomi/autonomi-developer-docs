# Sweep — 2026-08-20

## Branch

`claude/sweep-2026-08-20`

This run opened both a sweep PR (this summary) and a prose PR
(`claude/prose-2026-08-20-finalize-empty-tx-hashes`). The sweep PR carries the
metadata-only stamps; the prose PR carries the pages whose documented surface
changed. The two PRs never touch the same page.

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-sdk | 8378338 | a4accf1 |

Only `ant-sdk` drifted this run. Every other recorded repo (`ant-client`,
`ant-node`, `ant-protocol`, `self_encryption`, `evmlib`, `saorsa-core`,
`saorsa-transport`) is unchanged against its recorded SHA.

## Classification

One developer-facing surface changed and is handled entirely in the prose PR:
the external-signer wave-batch finalize now accepts an empty `tx_hashes` object
when the prepare step reported no payments (every chunk already stored). Every
other drifted record is **metadata-only** and is stamped here. No manual-review
issues were opened — no record was ambiguous.

- **ant-sdk** (`8378338 → a4accf1`): the `#239`
  (`bug/grpc-empty-tx-hashes-finalize`) merge lets `/v1/upload/finalize` and the
  gRPC `FinalizeUpload` accept an empty `tx_hashes` map for a wave-batch upload
  when prepare reported no payments — the all-already-stored case — finalizing
  without any on-chain payment. The change touches `antd/openapi.yaml`,
  `antd/proto/antd/v1/upload.proto`, `antd/src/rest/upload.rs`, and
  `antd/src/grpc/service.rs`. That surface is documented on
  `use-external-signers-for-upload-payments.md`, `rest-api.md`, and
  `grpc-services.md`, which the prose PR updates. The single-chunk finalize
  (`/v1/chunks/finalize`, `FinalizeChunk`) and its `tx_hashes` schema are
  unchanged. The MCP `finalize_upload` tool description on
  `mcp-server-reference.md` makes no claim the diff invalidates and the MCP
  crate is untouched in this window, so that page is metadata-only. Every other
  ant-sdk-citing page (language bindings, install, daemon command reference,
  store/retrieve pages, core-concepts, guides) documents surfaces the diff does
  not touch, so those are metadata-only.

## Pages updated (45 pages, 45 blocks)

For each drifted page, only the `<!-- verification: ... -->` block whose
`source_commit` matched the recorded `ant-sdk` SHA had its `source_commit` and
`verified_date` refreshed. Blocks for non-drifted repos (for example the
`ant-client` and `evmlib` blocks on multi-repo pages) were left byte-identical.
The three prose-PR pages
(`use-external-signers-for-upload-payments.md`, `rest-api.md`,
`grpc-services.md`) are not touched by this PR. No prose changed here.

## Skill

`skills/start/version.json` and the `skills/start/SKILL.md` frontmatter had the
`ant-sdk` value in their `verified_commits` maps refreshed (key set unchanged;
no other repo drifted). The `SKILL.md` body treats prepare/finalize payloads and
wave-batch response shapes as volatile ("fetch the live reference") and names no
surface that changed in this window, so the body is unchanged. The `version`,
`published_date`, and `CHANGELOG.md` are unchanged — this is a metadata-only
skill stamp, so the linked skill release is not triggered.

## Target-manifest

No `target-manifest` blocks are active; nothing was skipped on that basis.
