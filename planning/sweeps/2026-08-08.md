# Sweep — 2026-08-08

## Branch

`claude/sweep-2026-08-08`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-node | 9a50c6b | 20db1f9 |

## Classification

All records on the 9 pages updated here are **metadata-only**. The `ant-node` diff (`9a50c6b → 20db1f9`) does not touch a developer-facing surface — no documented command, endpoint, type, field, code sample, CLI flag, public Rust module, REST payload, or gRPC service changed on any recorded page.

- **ant-node** (`9a50c6b → 20db1f9`): the tree diff between the two SHAs is confined to the node-internal replication audit protocol and its tests — `src/replication/protocol.rs`, `src/replication/commitment_state.rs`, `src/replication/storage_commitment_audit.rs`, `tests/poc_audit_handler_live.rs`, and the in-repo ADR `0009-audit-proof-shape-and-protocol-families.md`. The change replaces two hand-kept wire-size ceiling matches with a single `ceiling_for` helper and makes `family_of_variant` return `Option<BodyFamily>`, so an unclassified body-variant index takes the strict subtree-audit ceiling at both encode and decode instead of the generous core allowance. All touched items are `pub(crate)` or private; no public API signature (`NodeBuilder`, `NodeConfig`, or any exported type the Direct Rust node page cites) changed, and no OpenAPI, `.proto`, or CLI surface changed. No recorded page documents the replication wire protocol, so the change is invisible to the rendered docs. No prose PR and no manual-review issues were opened.

## Pages updated (9)

The drifted `ant-node` `<!-- verification: ... -->` block on each page had its `source_commit` and `verified_date` refreshed. Non-drifted blocks (for example the `ant-sdk`, `ant-client`, `ant-protocol`, `evmlib`, `self_encryption`, and saorsa-family blocks on the multi-repo pages) were left byte-identical. No prose changed.

- `docs/architecture/system-overview.md`
- `docs/core-concepts/payment-model.md`
- `docs/guides/overview.md`
- `docs/guides/set-up-a-local-network.md`
- `docs/guides/test-your-application.md`
- `docs/reference/glossary.md`
- `docs/reference/source-repositories.md`
- `docs/rust/embed-a-node-in-your-application.md`
- `docs/sdk/start-the-local-daemon.md`

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-node` `verified_commits` value refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep).

## Note on the date suffix

The routine's real-date summary slots for 2026-08-04 through 2026-08-07 (branches `claude/sweep-2026-08-04` … `claude/sweep-2026-08-07`) were already consumed by merged sweeps earlier. This ant-node drift is separate, unprocessed work, so it takes the next free date suffix (`2026-08-08`) to satisfy the sweep-guard's requirement that the summary file be added at `planning/sweeps/<branch-date>.md`. The `verified_date` stamps on the bumped blocks match that suffix.
