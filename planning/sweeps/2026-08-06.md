# Sweep — 2026-08-06

## Branch

`claude/sweep-2026-08-06`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-node | db4c851 | 9a50c6b |

## Classification

All records on the 9 pages updated here are **metadata-only**. The `ant-node` diff (`db4c851 → 9a50c6b`) does not touch a developer-facing surface — no documented command, endpoint, type, field, code sample, CLI flag, public Rust module, or REST payload changed on any recorded page.

- **ant-node** (`db4c851 → 9a50c6b`): the diff is a release bump to `0.17.0` plus a change confined to `src/payment/verifier.rs` that adds a receiver-side price floor keyed to the close group's median commitment-bound price, alongside ADR documentation updates (`docs/adr/ADR-0006`, `docs/adr/ADR-0008`, new `docs/adr/ADR-0010`). The floor is an internal node-side economic admission mechanism gated behind an environment variable and defaulting to shadow mode (computed and logged, not enforced), so it changes no client-facing behaviour: a client still pays the on-chain market price when uploading, and quote collection, payment construction, and payment verification present the same surface. The node-side pricing formula documented on `payment-model.md` (`BASELINE + K × (n / D)^2`) lives in `ant-protocol`, which this diff does not touch, so that claim is unaffected. No CLI, daemon REST endpoint, public library API, binary entry point, or README surface changed. No recorded page documents the price floor, the group-median reference, or the internal verifier.

No prose PR and no manual-review issues were opened.

## Pages updated (9)

The drifted `ant-node` `<!-- verification: ... -->` block on each page had its `source_commit` and `verified_date` refreshed. Non-drifted blocks (for example the `ant-sdk`, `ant-client`, `ant-protocol`, `evmlib`, and `self_encryption` blocks on the multi-repo pages) were left byte-identical. No prose changed.

- `docs/architecture/system-overview.md`
- `docs/core-concepts/payment-model.md`
- `docs/guides/overview.md`
- `docs/guides/set-up-a-local-network.md`
- `docs/guides/test-your-application.md`
- `docs/reference/glossary.md`
- `docs/reference/source-repositories.md`
- `docs/rust/embed-a-node-in-your-application.md`
- `docs/sdk/start-the-local-daemon.md`

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-node` `verified_commits` value refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep).
