# Upstream sweep — 2026-06-13

## Drifted repos

| Repo | Recorded SHA | Head SHA | Classification |
|------|-------------|---------|----------------|
| ant-protocol | `ffaf12d41c1f129ed92051e04aaf5d6f8e94eec7` | `34c11cb9f626290b18263ebe8e10e172e5523424` | metadata-only |
| saorsa-core | `a681772c042b8215f36a9a860510d1fdb54b7540` | `180454671b87debcb2afbd83f2d8126c41b0dd02` | metadata-only |
| saorsa-transport | `2ee8e944ae32c3667ca8b588365a65a41f4b0f14` | `e24ecaced902f668d64cda2269deef0babd5d9ab` | metadata-only |

Not drifted: `ant-sdk`, `ant-client`, `ant-node`, `ant-merkle`, `ant-keygen`, `evmlib`, `self_encryption`, `saorsa-pqc`.

Pinned by target-manifest, not bumped: none (no active target-manifest).

## Developer-facing impact

Internal refactors; no developer-facing impact.

- **ant-protocol** (`rc-2026.6.2`): Version bump from 2.1.2 to 2.1.3 and dependency pin of `saorsa-core` updated from `0.24.5` to `0.25.0`. No wire-format, API, or protocol changes.
- **saorsa-core** (`rc-2026.6.2`): Major internal refactor removing `BootstrapManager` and `JoinRateLimiter` APIs. Bootstrap is now configured via `NodeConfig::builder()` with `bootstrap_peer()` and `close_group_cache_dir()`. Rate limiting removed from the public API. None of the docs pages reference `BootstrapManager` or `JoinRateLimiter` directly; the high-level description ("P2P node, DHT, bootstrap handling, trust system") remains accurate.
- **saorsa-transport** (`rc-2026.6.2`): Bootstrap cache feature removed entirely (`src/bootstrap_cache/` deleted, README Bootstrap Cache section removed). The docs pages do not document the bootstrap cache; transport descriptions (QUIC, NAT traversal, relay-assisted connectivity, pure post-quantum with ML-KEM-768 + ML-DSA-65) remain accurate.

## Outcome

6 docs pages stamped; `skills/start/version.json` and `skills/start/SKILL.md` verified_commits updated for `ant-protocol`.
