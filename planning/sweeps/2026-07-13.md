# Upstream sweep — 2026-07-13

Daily upstream-drift sweep. Metadata-only stamp refreshes for pages whose audited upstream changes carry no developer-facing impact on the documented surface. The new `ant node dismiss` command and the updated `ant node status` output on `ant-client` are handled separately in the prose PR for `docs/cli/command-reference.md`.

## Repos checked

| Repo | Range | Classification |
|------|-------|----------------|
| ant-sdk | e562923 → 9ff4ca8 | metadata-only |
| ant-client | 95a23be → bcab72a | prose for `docs/cli/command-reference.md` (separate PR); metadata-only for all other pages |
| ant-node | 3df630c → bc4c448 | metadata-only |
| ant-protocol | 82cab84 → 83d588e | metadata-only |
| saorsa-core | ef00920 → d323c46 | metadata-only |
| saorsa-transport | a24065b → 95efb84 | metadata-only |
| evmlib | 225acbb → 28fc354 | metadata-only |

## Developer-facing change

- **ant-sdk**: work on this range is confined to the native FFI crate (`ffi/`) — file cost estimation, external-signer payment plumbing, upload/download progress callbacks, and LAN-devnet manifest connect — plus dependency bumps in `antd` and upstream mobile-quickstart redirects. The REST/gRPC daemon surface (`antd/openapi.yaml`, `antd/proto/`) and the thin-client binding packages (`antd-py`, `antd-js`, and the rest) are byte-identical across the range, so no stamped SDK, MCP, or language-binding page changed.
- **ant-client**: introduces the `ant node dismiss` command and folds fleet-health and node-eviction reporting into `ant node status`; both are documented in the prose PR. The rest of the range is internal `ant-core` work (ADR-0004 commitment-bound quote plumbing, adaptive-limiter and Merkle-store refinements, low-disk node eviction, shrunk-DataMap resolve). The documented Rust error variants, `--upgrade-channel` flag, and payment-model claims are unchanged.
- **ant-node**: ADR-0004 commitment-bound quote pricing, first-audit scheduler observability, cumulative replication traffic accounting, and low-disk node eviction. The documented node-side pricing formula `BASELINE + K × (n / D)²`, payment modes, and cost-estimation surface are unchanged.
- **ant-protocol**: ADR-0004 shared commitment and pricing modules plus a `2.3.0` release bump. Changes are internal quote-signing and payment-proof structures; no documented protocol surface changed.
- **evmlib**: ADR-0004 adds `committed_key_count` and `commitment_pin` fields to the internal `PaymentQuote` and Merkle candidate types, plus a `commitment_sidecars` field on the Merkle proof, and a `0.9.0` release bump. The on-chain external-signer surface documented in the guides (`pool_commitments`, `payForMerkleTree`, the `MerklePaymentMade` event, `winner_pool_hash`) is unchanged.
- **saorsa-core**: cumulative wire-traffic accounting, per-type DHT message counting, a dependency bump, and dead-code lint fixes. Internal telemetry; no documented-surface change.
- **saorsa-transport**: cumulative relay and endpoint traffic accounting, NAT-traversal accounting additions, dependency bumps, and clippy/CI cleanups. The packet-crypto change is a readability refactor with no algorithm change. No documented-surface change.

## Target-manifest entries

None. No `target-manifest` blocks were skipped or bumped.

## Pages stamped

Verification blocks refreshed to the audited head SHAs across architecture, CLI (`use-the-cli`), core-concepts, guides, MCP, reference, rust, and sdk pages, plus the `verified_commits` maps in `skills/start/version.json` and `skills/start/SKILL.md`. `docs/cli/command-reference.md` is intentionally excluded here and carried in the prose PR.
