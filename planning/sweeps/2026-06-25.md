# Upstream sweep — 2026-06-25

## Drifted repos

| Repo | Recorded SHA | Head SHA | Classification |
|------|-------------|---------|----------------|
| ant-client | `4d0448458ec302af68a5504c533d105b0991c93c` | `3ab50c44a6ba62b34e879b86c97eae469b56cb3c` | prose (CLI command reference) + metadata-only (all other pages) |
| ant-node | `c53665bf4dda87cdbcf848606fa0182e822297d7` | `ed34951013fe7d96f25279f8e004e4168ea87e28` | metadata-only |
| saorsa-core | `dff6b0ec43680cfe7b026ba42ffd90501734e94d` | `fe74f93ab9e947ea08c1c0a168205b7011a4383a` | metadata-only |

Not drifted: `ant-sdk`, `ant-protocol`, `self_encryption`, `evmlib`, `ant-keygen`, `ant-merkle`, `saorsa-transport`, `saorsa-pqc`.

Pinned by target-manifest, not bumped: none (no active target-manifest).

## Developer-facing impact

- **ant-client**: `feat/file-download-peer-diagnostics` (plus ADR-governance tooling). Adds an `--all-peers` flag (alias `--try-all-peers`) to `ant file download` that fetches each file chunk from every selected closest peer and prints ranked per-peer diagnostics after a successful download, and adds a `chunk_peer_check` object to the JSON download output. This is a developer-facing CLI surface and is documented in the linked prose PR for `docs/cli/command-reference.md`. The remaining changes (deeper `ant-core` download internals, new `FileChunkPeerReport*` types, ADR governance files) do not alter any other documented surface, so every other ant-client page is a metadata-only stamp refresh.
- **ant-node**: `audit-on-gossip` / release v0.14.0. Internal replication and storage-commitment audit subsystem (ADR-0002: gossip-triggered contiguous-subtree storage audit and responsible-chunk restore), plus a deletion-aware quote-pricing resync (`QuoteGenerator::resync_records`) that keeps the quoted price aligned with the records a node actually holds. All internal node behavior; the pay-once model, Autonomi Network Token (ANT) payments, and Merkle batch payment surfaces a developer interacts with are unchanged.
- **saorsa-core**: `feat/log-trust-kclosest-swaps`. Observability-only logging for trust and K-closest peer swaps in the adaptive DHT and routing layers. No change to documented routing, trust, or architecture behavior.

## Outcome

21 docs pages stamped (metadata-only). Skill `verified_commits` and `verified_date` updated in `skills/start/version.json` and `skills/start/SKILL.md`. The ant-client CLI download-diagnostics prose change for `docs/cli/command-reference.md` is handled in the linked `claude/prose-2026-06-25-cli-file-download-all-peers` PR, which also carries that page's stamp refresh; the two PRs do not touch the same page.
