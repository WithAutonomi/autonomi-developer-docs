# Sweep — 2026-07-23

## Branch

`claude/sweep-2026-07-23`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-client | 9a4d8c4d | 24989150 |

## Classification

All records on the 19 pages updated here are **metadata-only**. The upstream change carries no developer-facing prose impact on any recorded page.

- **ant-client** (`9a4d8c4d → 24989150`, PR #150 `fix/single-node-min-quotes`): the diff is confined to `ant-core/src/data/client/{batch.rs,file.rs,payment.rs,quote.rs}`, `ant-core/src/data/mod.rs`, and the `ant-core` payment/security e2e tests. It refactors the single-node payment path: the quote collector no longer requires a full close group of quotes before it can build a single-node payment (it proceeds from at least one witnessed quote), and the payment builder moves from `ant_protocol::payment::SingleNodePayment` to a new `ant_core::data::SingleNodeQuotePayment` type, with the median-quote 3x multiplier lifted into a named `SINGLE_NODE_PAYMENT_MULTIPLIER` constant. None of these symbols (`SingleNodePayment`, `SingleNodeQuotePayment`, `CLOSE_GROUP_SIZE`, `SINGLE_NODE_PAYMENT_MULTIPLIER`) appear in any recorded docs page or in `SKILL.md`. The documented Direct Rust surface is unchanged at the head SHA: `PaymentIntent` still carries `payments` and `total_amount`, `ExternalPaymentInfo::WaveBatch { prepared_chunks, payment_intent }` and `::Merkle { prepared_batch, .. }` are intact, and the `PaymentMode` (`Auto`/`Merkle`/`Single`), `ClientConfig`, `PreparedMerkleBatch`, and `Visibility` items cited in the library reference are untouched by this range. No CLI command, flag, or `--help` surface changed.

No prose PR and no manual-review issues were opened.

## Pages updated (19)

Every drifted `<!-- verification: ... -->` block on these pages had its `source_commit` and `verified_date` refreshed. The non-`ant-client` blocks (for example the `ant-sdk`, `ant-node`, `ant-protocol`, `evmlib`, and `self_encryption` blocks on the multi-repo reference, architecture, and payment pages) were left byte-identical. No prose changed.

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-client` `verified_commits` value refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep).
