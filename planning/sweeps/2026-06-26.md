# Upstream sweep — 2026-06-26

## Drifted repos

| Repo | Recorded SHA | Head SHA | Classification |
|------|-------------|---------|----------------|
| ant-node | `ed34951013fe7d96f25279f8e004e4168ea87e28` | `0be9579c679e07fa266207bb9af5a4dde1aaee5a` | metadata-only |

Not drifted: `ant-sdk`, `ant-client`, `ant-protocol`, `self_encryption`, `evmlib`, `ant-keygen`, `ant-merkle`, `saorsa-core`, `saorsa-transport`, `saorsa-pqc`.

Pinned by target-manifest, not bumped: none (no active target-manifest).

## Developer-facing impact

- **ant-node**: `fix/concurrent-inbound-replication` (PR #158). The responsible-chunk audit (audit #2) responder is now spawned onto a detached task off the serial replication message loop, with the same flood-fair admission control (global ceiling plus per-peer cap) already used by the subtree and byte audit responders, removing head-of-line blocking on replication traffic. The concurrent audit-responder ceiling (`MAX_CONCURRENT_AUDIT_RESPONSES`) was raised from 8 to 16. Both changes are internal node behavior in `src/replication/config.rs` and `src/replication/mod.rs`. No documented surface — the pay-once model, data storage and retrieval flows, CLI, SDK, and Direct Rust APIs a developer interacts with — is affected.

## Outcome

9 docs pages stamped (metadata-only): `docs/architecture/system-overview.md`, `docs/core-concepts/payment-model.md`, `docs/guides/overview.md`, `docs/guides/set-up-a-local-network.md`, `docs/guides/test-your-application.md`, `docs/reference/glossary.md`, `docs/reference/source-repositories.md`, `docs/rust/embed-a-node-in-your-application.md`, `docs/sdk/start-the-local-daemon.md`. The skill `verified_commits` entry for `ant-node` and the `verified_date` were refreshed in `skills/start/version.json` and `skills/start/SKILL.md`. No prose changed; no manual-review issues opened.
