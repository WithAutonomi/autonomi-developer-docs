# Sweep — 2026-07-24

## Branch

`claude/sweep-2026-07-24`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-sdk | 71c84b90 | 09da0f4c |
| ant-node | eb74b564 | 2dac0362 |
| saorsa-transport | d9e1687c | 5ba61f27 |

## Classification

All records on the 50 pages updated here are **metadata-only**. Each upstream change is internal and carries no developer-facing prose impact on any recorded page.

- **ant-sdk** (`71c84b90 → 09da0f4c`, PR #226 `claude/rc-bump-ant-ffi-0.0.8-rc.1`): the diff is confined to `antd/Cargo.toml`, `antd/Cargo.lock`, `ffi/rust/ant-ffi/Cargo.toml`, and `ffi/rust/Cargo.lock`. It is a dependency-version bump: `antd` `0.11.1 → 0.11.2`, the `ant-core` git dependency moves to tag `ant-cli-v0.3.1`, `evmlib` `0.8.1 → 0.9.0`, and `ant-protocol` `2.2.2 → 2.3.0`. No `antd/openapi.yaml`, `.proto`, handler source, or CLI surface changed in this range, so every documented REST endpoint, gRPC service, message type, field, and language-binding mapping is byte-identical at the head SHA. None of the bumped version strings appears in any recorded page.

- **ant-node** (`eb74b564 → 2dac0362`, PR #183 `fix/first-audit-admission-cap`): `ant-node` `0.14.4 → 0.15.0`. The diff reworks node-internal replication (`src/replication/{mod.rs,config.rs,protocol.rs}`), extends payment-proof verification with the ADR-0004 commitment-bound quote-pricing binding (`src/payment/verifier.rs`, new `docs/adr/ADR-0004-commitment-bound-quote-pricing.md`, status *Proposed*), and adds first-audit admission-cap e2e tests. The public storage-pricing formula (`ant_protocol::payment::pricing::calculate_price`, re-exported from `src/payment/pricing.rs`) is unchanged in this range, so the `BASELINE + K × (n / D)^2` description on the payment-model page still holds. The only docs references to `ant-node` replication and payment verification are the high-level system-overview and source-repositories lines, which remain accurate. No documented endpoint, command, type, field, or code sample changed.

- **saorsa-transport** (`d9e1687c → 5ba61f27`, PR #128 `fix/masque-joinhandle-double-poll`): a single-file bug fix in `src/masque/relay_server.rs` that stops the relay forwarding cleanup from awaiting a `JoinHandle` that already completed through `select!`, plus three regression tests. The documented transport claims (QUIC transport, NAT traversal, relay-assisted fallback) are unchanged.

No prose PR and no manual-review issues were opened.

## Pages updated (50)

Every drifted `<!-- verification: ... -->` block on these pages had its `source_commit` and `verified_date` refreshed. Non-drifted blocks on multi-repo pages (for example the `ant-client`, `evmlib`, `ant-protocol`, `saorsa-core`, and `self_encryption` blocks on the architecture, payment, glossary, and source-repositories pages) were left byte-identical. No prose changed.

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-sdk` and `ant-node` `verified_commits` values refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep). `saorsa-transport` is not tracked in the skill `verified_commits` map, so no skill entry moved for it.
