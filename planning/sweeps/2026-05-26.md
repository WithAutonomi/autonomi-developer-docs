# Upstream sweep — 2026-05-26

## Run summary

- **Scanner status**: ok
- **Total records**: 121
- **Drifted**: 90 across 5 repos (ant-sdk, ant-client, ant-node, ant-protocol, saorsa-core)
- **Not drifted**: 31

## Upstream changes

### ant-sdk (`a3cf4e40` → `7a113b390522d76d28b8f3e5b4078f9c9418d46f`)

Major REST API v2 rename — private data and file endpoints renamed across OpenAPI spec, proto files, all 15 language SDK bindings, and the MCP server:

- `POST /v1/data/private` → `POST /v1/data` (private data upload)
- `GET /v1/data/private?data_map=` → `POST /v1/data/get` with JSON body (private data download)
- `POST /v1/files/upload/public` → `POST /v1/files/public`
- `POST /v1/files/download/public` → `POST /v1/files/public/get`
- Added `POST /v1/files` (private file upload, returns `data_map`)
- Added `POST /v1/files/get` (private file download, uses `data_map` in body)
- `payment_mode` parameter added to cost endpoints
- SDK method renames: `data_put_private` → `data_put`, `data_get_private` → `data_get`, `file_upload_public` → `file_put_public`, etc.
- MCP `upload_file`/`download_file`: `is_directory` removed, `private` param added
- `store_data` MCP response: `chunks_stored` + `payment_mode_used` replaces `cost`
- gRPC proto: Data service `Put`/`Get` methods renamed; File service `Put`/`PutPublic`/`Get`/`GetPublic`/`Cost` added

### ant-client (`3df67642` → `e67472424f94acd4b9188a342271210d4ab9f94d`)

One commit (merkle payment preflight resume fix). CLI version bump 0.2.3 → 0.2.4. No developer-facing API changes.

### ant-node (`f38fdcac` → `2a8b91deada5506c72b7d234655119b2ab803d92`)

One commit (strip unused quoting metrics, internal refactor). No developer-facing changes.

### ant-protocol (`cbaf710d` → `83b6b4e2b12c217fe2728cd6bd9d923e50b86708`)

Cargo.lock update only. No API changes.

### saorsa-core (`550183b7` → `b4d0da07e75e85e4473b50e33d63a6648afc4dd2`)

One commit (rc-2026.5.3, internal DHT address filtering). No public API changes.

## Classification

| Classification | Count | Pages |
|---|---|---|
| Prose | 9 | rest-api.md, grpc-services.md, mcp-server-reference.md, store-and-retrieve-data.md, data-types.md, build-read-only-features.md, sdk/reference/overview.md, python.md, SKILL.md |
| Metadata-only | 45 pages | all other drifted pages (see sweep PR) |
| Ambiguous | 0 | — |
| Manual review needed | 0 | — |

## PRs opened

- `claude/sweep-2026-05-26` — metadata-only verification stamp refresh for 45 pages (sweep PR, ready-for-review)
- `claude/prose-2026-05-26-private-data-api-renames` — prose + verification refresh for 9 pages including skill 0.1.6-draft bump (draft PR)

## Skill release

Skill bumped from `0.1.5-draft` to `0.1.6-draft`. `version.json` and `CHANGELOG.md` updated in prose PR.

## Target-manifest pins

None skipped (no target-manifest.yml active).
