# Sweep — 2026-08-05

## Branch

`claude/sweep-2026-08-05`

## Repos bumped

| Repo | Recorded SHA | Head SHA |
|------|-------------|----------|
| ant-node | 0c6977a | db4c851 |

## Classification

All records on the 9 pages updated here are **metadata-only**. The diff touches no developer-facing surface — no documented command, endpoint, type, field, code sample, CLI flag, public Rust module, or README changed on any recorded page.

- **ant-node** (`0c6977a → db4c851`): internal replication repair hardening (`src/replication/` — admission, audit, bootstrap, pruning, quorum, scheduling, possession, protocol families), storage internals (`src/storage/lmdb.rs`), the ADR-0004 quote-arithmetic enforcement in the payment verifier (`src/payment/verifier.rs`), several ADR documents, and test suites. The public embedding API is unchanged: `src/lib.rs`, `src/node.rs`, `src/config.rs`, and `src/payment/pricing.rs` are byte-identical across the range, so `NodeBuilder`, `NodeConfig`, `NodeConfig::development()`, `RunningNode::subscribe_events()`, the config fields, and the node-side storage pricing formula all still hold. The payment-proof verification the page describes (signature checks, on-chain verification, record-level validation) is unchanged; the ADR-0004 arithmetic gate is an additional internal check, not a new client-facing surface. The crate version stays `0.16.0` in source; the docs continue to cite the installable crates.io release. The two high-level page mentions of replication (`system-overview.md`, `source-repositories.md`) remain accurate.

No prose PR and no manual-review issues were opened.

## Pages updated (9)

The drifted `ant-node` `<!-- verification: ... -->` block on each page had its `source_commit` and `verified_date` refreshed. Non-drifted blocks (for example the `ant-sdk`, `ant-client`, `ant-protocol`, `evmlib` blocks on the multi-repo pages) were left byte-identical. No prose changed.

- `docs/architecture/system-overview.md`
- `docs/core-concepts/payment-model.md`
- `docs/guides/overview.md`
- `docs/guides/set-up-a-local-network.md`
- `docs/guides/test-your-application.md`
- `docs/reference/glossary.md`
- `docs/reference/source-repositories.md`
- `docs/rust/embed-a-node-in-your-application.md`
- `docs/sdk/start-the-local-daemon.md`

## Skill

`skills/start/version.json` and `skills/start/SKILL.md` frontmatter had their `ant-node` `verified_commits` value refreshed, and the `SKILL.md` frontmatter `verified_date` was updated. The `SKILL.md` body, `version`, `published_date`, and `CHANGELOG.md` are unchanged (metadata-only sweep).
